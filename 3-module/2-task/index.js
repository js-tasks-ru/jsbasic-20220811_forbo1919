function filterRange(arr, a, b) {
  // ваш код...
  // var 1

  // let result = [];  

  // arr.forEach(item => { 
  //   if( item >= a && item <= b ) 
  //   result.push(item)
  // })

  // return result;

  // var 2
  return arr.filter(item => item >= a && item <= b);

}
